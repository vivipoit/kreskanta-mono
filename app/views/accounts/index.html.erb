<div class="container-fluid mt-3">
  <h2><%= icon('fas', 'wallet', 'Account Info', class: 'm-1') %></h2>
  <p class="m-0 p-0">BinanceClient.account_info(current_user)</p>
  <p class="m-0 p-0 text-muted">https://binance-docs.github.io/apidocs/spot/en/#account-information-user_data</p>
</div>

<div class="container-fluid mt-3">
  <div class="row">
    <div class="col-12">
      <h4>Balances</h4>
    </div>
  </div>
  <div class="row">
    <% @info.fetch(:balances, {}).each do |balance| %>
      <% next if balance_zero?(balance) %>
      <div class="col-md-3">
        <div class="card m-1">
          <div class="card-header text-center">
            <h5 class="card-title"><%= balance[:asset] %></h5>
          </div>
          <div class="card-body text-end">
            <p class="card-text text-success">
              <%= icon('fas', 'lock-open', class: 'm-1 float-start') %>
              <%= balance[:free] %>
            </p>
            <p class="card-text text-danger">
              <%= icon('fas', 'lock', class: 'm-1 float-start') %>
              <%= balance[:locked] %>
            </p>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>

<div class="container-fluid mt-3">
  <h4>All Info</h4>
  <% @info.each do |key, value| %>
    <div class="card my-1">
      <div class="card-header">
        <h5 class="card-title"><%= key %></h5>
      </div>
      <div class="card-body">
        <p class="card-text"><%= value.inspect %></p>
      </div>
    </div>
  <% end %>
</div>
